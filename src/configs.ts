export const token = '' // bot toke id
export const prefix = '' // bot prefix
export const mainServer = '' // server id in witch the bot will send the messages of a user Dm
export const parentChannel = '' // the catagory that the channels will be created at
export const actionLogChannel = '' // channel id on witch events are logged of wht happen with the bot (optional)
